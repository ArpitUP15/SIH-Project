services:
  - type: web
    name: mindease-backend
    env: python
    plan: free
    rootDir: backend
    buildCommand: "pip install -r requirements.txt && python mindease/manage.py collectstatic --noinput"
    startCommand: "gunicorn mindease.wsgi:application --chdir mindease"
    postDeployCommand: "python mindease/manage.py migrate --noinput"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "*"
      - key: DATABASE_URL
        fromDatabase:
          name: mindease-db
          property: connectionString
    autoDeploy: true
databases:
  - name: mindease-db
    plan: free

